{
  "name": "@retrobeam/worker",
  "private": true,
  "type": "module",
  "license": "AGPL-3.0-or-later",
  "scripts": {
    "check": "tsc --noEmit",
    "types": "wrangler types --strict-vars=false",
    "test": "vitest run"
  },
  "dependencies": {
    "@retrobeam/shared": "workspace:*",
    "hono": "^4.13.5",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.22.0",
    "typescript": "^5.9.3",
    "vitest": "^4.1.11",
    "wrangler": "^4.128.0"
  }
}
